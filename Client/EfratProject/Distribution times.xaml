<Page x:Class="EfratProject.Distribution_times"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EfratProject"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Distribution_times">

    <!--<Grid Margin="113,10,209,83">
        <Image Source="/pictures/22אזורי וזמני אספקה.PNG"  Width="350" />
    </Grid>-->
    <StackPanel FlowDirection="RightToLeft">
        <!--<TextBlock Text="אזורים וימי חלוקה"/>-->
        <TextBlock Text="אזורים וימי חלוקה" Background="Beige" FontSize="17" HorizontalAlignment="Center"  />

        <ListView x:Name="LstCity" Width="auto" FontSize="15" HorizontalAlignment="Center" >
            <ListView.View>
                <GridView >
                    <GridViewColumn Header="שם העיר" DisplayMemberBinding="{Binding CityName}" Width="100"/>
                    <!--<GridViewColumn Header="יום חלוקה" DisplayMemberBinding="{Binding DayInWeek}"/>-->
                    <GridViewColumn Header="יום חלוקה" Width="100">
                        <GridViewColumn.DisplayMemberBinding>
                            <Binding Path="DayInWeek">
                                <Binding.Converter>
                                    <local:ConvertDays/>
                                </Binding.Converter>
                            </Binding>
                        </GridViewColumn.DisplayMemberBinding>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>
</Page>
