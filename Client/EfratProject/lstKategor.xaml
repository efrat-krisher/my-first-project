<Page x:Class="EfratProject.lstKategor"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EfratProject"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="lstKategor">

    <StackPanel FlowDirection="RightToLeft">
        <DockPanel Background="Beige">
               <TextBlock Text="רשימת קטגוריות" Background="Beige" FontSize="15" />
            <Button Content="חזור לתפריט" Width="116" FontSize="20"  Click="Back" HorizontalAlignment="Right"/>
        </DockPanel>
        <Button Content="הוספת קטגוריה" Click="NewKategor" FontSize="15" />
        <ListView x:Name="LstKategor"  Width="auto" FontSize="15" >
            <ListView.View>
                <GridView >
                    <GridViewColumn Header="קוד קטגוריה"  DisplayMemberBinding="{Binding KategorCode}" Width="85"/>
                    <GridViewColumn Header="שם קטגוריה" DisplayMemberBinding="{Binding KategorName}" Width="85"/>
                    <GridViewColumn Header="תמונה" Width="85"  >
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Image>
                                    <Image.Source>
                                        <Binding Path="KategorPicture">
                                            <Binding.Converter>
                                                <local:PicturConvert/>
                                            </Binding.Converter>
                                        </Binding>
                                    </Image.Source>
                                </Image>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>
</Page>
