<Page x:Class="EfratProject.lstKosher"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EfratProject"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="lstKosher">

    <StackPanel FlowDirection="RightToLeft">
        <DockPanel>
            <WrapPanel>
                <Button  x:Name="delete" Content="מחיקת כשרות"  Width="80"   Click="DeleteKosher" Margin="2" Height="50"/>
                <Button Content="הוספת כשרות" Click="NewKosher" Width="80" Height="50"/>
            </WrapPanel>
            <WrapPanel HorizontalAlignment="Right">
                <Button Content="חזור לתפריט"  Width="116" Height="50"  FontSize="20"  Click="Back" HorizontalAlignment="Left" />
            </WrapPanel>
        </DockPanel>
       
            <TextBlock Text="רשימת כשרויות" Background="Beige" FontSize="17" />

            <ListView x:Name="LstKosher" Width="auto" FontSize="15" >
            <ListView.View>
                <GridView >
                    <GridViewColumn Header="קוד כשרות" DisplayMemberBinding="{Binding KosherCode}" Width="85"/>
                    <GridViewColumn Header="שם כשרות" DisplayMemberBinding="{Binding KosherName}" Width="85"/>
                    <GridViewColumn Header="תמונה" Width="85"  >
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Image>
                                    <Image.Source>
                                        <Binding Path="KosherPicture">
                                            <Binding.Converter>
                                                <local:PicturConvert/>
                                            </Binding.Converter>
                                        </Binding>
                                    </Image.Source>
                                </Image>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>
</Page>
